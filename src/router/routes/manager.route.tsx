import ManagerPage from '@app/pages/manager/manager.page';

import nav from '../navigation/manager.navigate';

export default {
    id: nav.index.id,
    path: nav.index.path,
    element: <ManagerPage />,
    children: [],
};
